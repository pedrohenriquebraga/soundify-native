<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Soundify" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="white">@color/black</item>
        <item name="black">@color/white</item>
        <item name="primary_color">@color/primary</item>
        <item name="secondary_color">@color/secondary</item>
        <item name="background">@color/background_dark</item>
        <item name="shape">@color/shape_dark</item>
    </style>
</resources>